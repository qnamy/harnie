---
name: dev-quick
description: "DEPRECATED alias (removed in 0.12) — merged into the single harnie:dev pipeline in 0.11. Invoke harnie:dev instead."
---

# dev-quick → harnie:dev (0.11 alias)

This track was merged into the single pipeline. First make the two entry judgments from `commands/dev.md` (provisional size by its S/M/L criteria — the alias name implies nothing — and run difficulty), then **immediately invoke the `harnie:dev` skill with the same task arguments**, and note the deprecation to the user in one line. Do no other work in this skill.
