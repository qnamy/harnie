---
description: harnie plan 트랙 강제 — 큰 작업(신규·구조변경)을 계획→설계리뷰→오케스트레이션→코드리뷰→최종 웨이브(Coverage·Quality·Runtime·Scope)로 처리
argument-hint: "<작업 설명>"
---

작업: $ARGUMENTS

`plan` 스킬을 invoke 해 위 작업을 plan 트랙으로 오케스트레이션한다(크기 재분류 없이 강제). 작업 인자를 그대로 스킬에 넘긴다.
