---
description: Force the harnie quick track for small tasks such as incidents and fixes, using a lightweight inline flow with stage-by-stage cross-model review
argument-hint: "<task description>"
---

Task: $ARGUMENTS

Invoke the `quick` skill to orchestrate the task through the quick track. Force this track without reclassifying task size, and pass the task argument to the skill unchanged.
